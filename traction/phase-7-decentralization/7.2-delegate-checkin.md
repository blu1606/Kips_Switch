# 7.2 Delegate Check-in

> ✅ **Already Implemented** in contract.

## 🎯 Goal
Allow a "hot wallet" (mobile/delegate) to ping on behalf of owner (cold wallet).

## 📐 Current Implementation

### Contract (Done ✅)
- `delegate: Option<Pubkey>` in Vault struct
- `set_delegate(new_delegate)` instruction
- `ping()` accepts owner OR delegate

### Frontend (Done ✅)
- `DelegateModal.tsx` - Set/clear delegate UI

## 📂 Files
- Contract: `deadmans-switch/programs/.../lib.rs`
- UI: `src/components/dashboard/DelegateModal.tsx`

## 🔜 Remaining (Optional)
- [ ] Better UX for delegate setup flow
- [ ] "Scan QR" to add mobile wallet as delegate
