# 14.2 Unit Testing Strategy (Logic)

> **Status:** Planned
> **Goal:** Ensure the correctness of critical utility functions and hooks.
> **Tool:** Vitest

## 🎯 Reach Coverage
- **`src/utils/crypto.ts`:** 100% Coverage. Verification of encryption/decryption roundtrips, key wrapping, and error handling.
- **`src/hooks/useSecurityScanner.ts`:** Verification of regex matching against PII patterns.
- **`src/utils/validation.ts`:** Input validation logic.

## 🛠️ Implementation
1.  Install: `yarn add -D vitest`
2.  Config: `vitest.config.ts` (mapped to `@/*`).
3.  Test Files: `*.test.ts` alongside source files or in `__tests__`.
